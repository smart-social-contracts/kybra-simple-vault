dfx canister call ckbtc_ledger icrc1_balance_of '(record { owner = principal "'"$PRINCIPAL"'"; subaccount = null })'
dfx canister call ckbtc_ledger icrc1_balance_of '(record { owner = principal "'"$VAULT_ID"'"; subaccount = null })'

dfx canister call vault get_stats

dfx canister call vault get_balance "$PRINCIPAL"
dfx canister call vault get_transactions "$PRINCIPAL"

dfx canister call vault update_transaction_history


AMOUNT=1100 dfx canister call ckbtc_ledger icrc1_transfer '(record {
	to = record {
		owner = principal "'"$VAULT_ID"'";
		subaccount = null
	};
	amount = '$AMOUNT';
	fee = null;
	memo = null;
	from_subaccount = null;
	created_at_time = null
})'


AMOUNT=1100 dfx canister call ckbtc_ledger icrc1_transfer '(record {
	to = record {
		owner = principal "'"$VAULT_ID"'";
		subaccount = null
	};
	amount = '$AMOUNT';
	fee = null;
	memo = null;
	from_subaccount = null;
	created_at_time = null
})'

